@using PagedList;
@using PagedList.Mvc;
@model PagedList.PagedList<WebsiteNhaHang.Models.SuKien>

@{
    ViewBag.Title = "Quản lý sự kiện";
    Layout = "~/Views/layout/Admin/_QuanLy.cshtml";
}
<div class="row-fluid">
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.First().TenSuKien)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().NgayBatDau)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().NgayKetThuc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().NgayDang)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().HinhAnh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().ChiTiet)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().NguoiDang)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.TenSuKien)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NgayBatDau)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NgayKetThuc)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NgayDang)
                </td>
                <td>
                    @*@Html.DisplayFor(modelItem => item.HinhAnh)*@
                    <img src="@Url.Content("~/Content/img/MonAn/KhuyenMai-Sukien/" + @item.HinhAnh)" style="width:70%" />

                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ChiTiet)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TaiKhoan.Email)
                </td>
                <td>
                    @Html.ActionLink("Sửa", "Edit", new { id = item.MaSuKien }) |
                    @Html.ActionLink("Xóa", "Delete", new { id = item.MaSuKien })
                </td>
            </tr>
        }

    </table>
    <div class="dataTables_paginate paging_bootstrap pagination align-center ml-2">
        @Html.ActionLink("Thêm Sự kiện", "Create")
    </div>
    <div class="row-fluid ml-2">
        <div class="span6">
            <div class="dataTables_info" id="sample-table-2_info">
                Trang: @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) / @Model.PageCount
            </div>
        </div>
        <div class="span6">
            <div class="dataTables_paginate paging_bootstrap pagination">
                <ul>
                    @Html.PagedListPager(Model, Page => Url.Action("Index", new { Page = Page }))
                </ul>
            </div>
        </div>
    </div>
</div>